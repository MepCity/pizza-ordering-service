apiVersion: v1
kind: Service
metadata:
  name: pizza-ordering-service
spec:
  selector:
    app: pizza-ordering-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
